# Root requirements so Railway's builder (Railpack/Nixpacks) detects Python and
# installs the eval-pipeline deps alongside the Node app on one service.
#
# Kept self-contained (NOT `-r eval_pipeline/requirements.txt`) because Railpack
# copies only this file into the install layer before running pip, so a nested
# `-r` include would fail (the referenced file isn't present yet). Keep in sync
# with eval_pipeline/requirements.txt.
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
scikit-learn>=1.3   # cohen_kappa_score (QWK) in metrics.py
anthropic>=0.40     # RealLLMClaude — the eval judge defaults to real claude-sonnet-4-6
