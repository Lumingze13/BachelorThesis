# Change record — 2026-06-11 (UI / prompts / admin diagnostics batch)

Requested by Kangzhi in chat (8-point list). The previous autonomous improve→deploy
iteration loop is **paused**; this is a single targeted batch. Branch:
`claude/beautiful-heisenberg-qpf2sw` → merged to `Kaehl` (Railway auto-deploys `Kaehl`).

中文摘要：本批修复了回复被截断的问题（max_tokens 1024 太小）、把 main/baseline
Stage-C prompt 改为强制短回复、每轮对话后显示可点的"问题灵感"卡片、修复 Restart
按钮遮挡侧栏文字、计时器在输入框下方常驻可见、默认阅读字体改为 sans、post-test
之后的自由对话现在会显示之前的全部聊天记录、admin 报错改为可定位原因的横幅。
Admin 的根本原因是 Railway 的 `DATABASE_URL` 指向 localhost —— 需要在 Railway
里改成 `${{Postgres.DATABASE_URL}}`（见 §9，代码改不了这个）。

---

## 1. Replies were suddenly cut off mid-sentence (用户痛点 #1)

**Root cause.** `server.js` capped every model call at `MAX_TOKENS = 1024`. The live
provider is gpt-5.1 via the UvA proxy — a *reasoning* model whose hidden reasoning
tokens count against the same cap, so even a moderately long visible reply ran out of
budget and was truncated mid-sentence.

**Fix.** `MAX_TOKENS` is now `Number(process.env.LLM_MAX_TOKENS ?? 4096)` (env-
overridable), and the OpenAI-compatible path logs a warning whenever
`finish_reason === 'length'` so a future cap problem is visible in Railway logs
instead of silent. File: `server.js`.

## 2. Replies were too long to read comfortably (用户痛点 #1bis)

**Fix.** The Stage-C **main** prompt's PACING section and the **baseline** prompt now
share a hard, *identical* length rule: at most 2 short paragraphs, ~60–110 words, one
idea/scene per message, at most one question back. The main prompt's COMPONENT 2 also
says a scene should land in 2–3 sentences. Identical wording in both conditions keeps
reply length from becoming a condition confound. Andrea's two Phase-B prompts
(reflective / direct) are **untouched**. File: `lib/prompt.js`.

## 3. Tappable question cards after every reply (用户痛点 #2)

**Fix.** A fixed, neutral pool of 18 questions (`ASK_POOL` in `chat.jsx`) covering
angles a participant may not think of (money, weekends, stress, regrets, first job,
paths not taken, …). After **every** future-self reply, 3 not-yet-used questions are
shown as chips above the composer ("Ideas to ask"); tapping one sends it as the user
turn; the pool recycles when exhausted; free typing always remains. Also added to the
free-continuation chat.

**Design note (deliberate):** the pool is static and identical across conditions —
NOT model-generated — so the nudges cannot differ between main and baseline (no new
confound). Files: `chat.jsx`, `styles.css` (`.ask-row`, `.ask-chip`; the old one-shot
`.suggested` block was removed).

## 4. Restart button covered the bottom-left explanation text (用户痛点 #3)

**Root cause.** The fixed-position `↻ Restart` fab sat exactly on the chat sidebar's
"Why this is a role-play" text.

**Fix.** On desktop chat (≥861px) the fab is hidden; the sidebar's top button (now
labelled "↻ Start over", previously "New session") does the same thing and now asks
for confirmation first (it used to restart instantly on a single click — data-loss
hazard). On mobile, where the sidebar is hidden, the fab remains. Files: `app.jsx`
(`confirmRestart` shared by fab + sidebar), `chat.jsx`, `styles.css`.

## 5. Timer not visible while chatting (用户痛点 #4)

**Fix.** The count-up clock now also renders in the composer footer (next to "Ask
anything…"), i.e. exactly where the participant is looking while typing. The header
clock stays. Still a count-UP clock — Build Plan §16 forbids countdown/pressure cues.
Files: `chat.jsx`, `styles.css`.

## 6. Default reading font → sans (用户痛点 #5)

**Fix.** `COMFORT_DEFAULTS.font` changed `'serif'` → `'sans'` and the localStorage
key bumped `thesis_comfort_v1` → `thesis_comfort_v2` so devices that stored the old
default pick up the new one (other comfort prefs reset once on first visit — accepted
trade-off). Serif remains available in the Aa comfort panel. File: `screens.jsx`.

## 7. Post-test chat looked like a brand-new conversation (用户痛点 #6)

**Root cause.** `FreeContinuation` reused the same server session (model memory was
intact) but rendered an empty thread.

**Fix.** `app.jsx` now passes the Phase-C transcript as a `history` prop;
`FreeContinuation` renders it read-only (slightly dimmed) above a quiet
"Your conversation continues" divider, auto-scrolled to the newest turn. Autosave
still records **only the new free-continuation turns** — the logging separation
(§3.9b: free chat never part of the main analysis) is preserved. Files: `app.jsx`,
`chat.jsx`, `styles.css`.

## 8. Admin: "List failed. — AggregateError" (用户痛点 #7)

**Root cause (live, verified via `/healthz`):** the Railway service variable
`DATABASE_URL` points at **localhost** (it matches the local-dev line in
`.env.example`). Inside the Railway container there is no localhost Postgres, every
connection is refused on all addresses, and Node surfaces that as `AggregateError`
with an empty message. **No code change can fix the live instance — see §9.**

**Code fixes (diagnosability):**
- `lib/db.js`: new `describeDbError()` unwraps AggregateError into the real causes
  (e.g. `connect ECONNREFUSED 127.0.0.1:5432`); new `dbHint()` returns an actionable
  fix line when `DATABASE_URL` is unset or points at localhost. `/healthz` now
  includes both.
- `lib/admin_routes.js`: all list/export/load error responses carry `detail` + `hint`.
- `admin/index.html`: a `DbHealthBanner` probes `/healthz` once and shows a single
  prominent banner ("Database not connected — … Fix: …") across all tabs instead of
  four cryptic per-tab failures; per-request errors also append the hint.

### 8b. Follow-ups found during live verification

- **Brevity needed structural enforcement.** Live tests on the deployed gpt-5.1
  showed the PACING rule alone still allowed 230–360-word replies (a casual "what's
  a normal Tuesday like" drew 357 words). Two reinforcements, identical across
  main/baseline: (a) the length rule now OPENS both Stage-C prompts as a
  non-negotiable that overrides later instructions; (b) `server.js` appends a
  call-time trailing system reminder on every phase-c completion (recency effect;
  never stored in the transcript; Phase B incl. Andrea's prompts untouched).
  **Verified live after deploy: 2 passes × 2 conditions, every reply 2 paragraphs /
  89–163 words, including under a "tell me everything" stress test, none truncated.**
- **`/admin?token=…` (and `/results?token=…`) bounced to login.** The page was
  served, but the app's own API calls carried no token (no cookie was ever set),
  got 401 and redirected to the login screen — part of the "admin 全报错" feel. A
  valid `?token=` now also sets the auth cookie. Files: `lib/admin_routes.js`,
  `lib/results_routes.js`.

---

# Batch 2 — same day, after receiving Build Plan v5.1 + Brief v4.1 (3 iterations, then paused)

## Spec audit of Batch 1 (user instruction: revert anything that violates the docs)

Verdict: **nothing reverted.** The three deviations from the as-built spec are all
explicit user orders, applied identically across conditions, touching none of the
protected elements (profile isolation §6, instrument wording §10, consent §15,
time policy §11.4, Andrea's prompts §8.4/8.5). They must be folded into the spec
as v5.1 → v5.2 amendments (see the integration instructions below):
1. Stage-C reply length — Build Plan §8.2/8.3 say "2-4 short paragraphs"; now ≤2
   paragraphs / ~60-110 words, no exceptions, + per-call reminder (user order).
2. §7/§16 "suggested first questions until the first send" — now "Ideas to ask"
   chips after every reply (user order).
3. §16 comfort default "reading font Serif" — now Sans (user order).
The free-continuation history display *fixes* a UI/spec mismatch (§7 Screen 8
promised "full history retained" but the old UI showed an empty thread).

## Iteration 1 — full-flow screenshot audit (1440px + 390px, mocked backend) + fixes

Tooling: `test/ui_walk.mjs` (Playwright; drives landing → consent → avatar →
pre-survey → pauses → phase B cards + lock-in → chat → post-survey → free
continuation → closure with all /api/* mocked; 34 screenshots per run).

Found and fixed:
- **Mobile sidebar bug (pre-existing, affects every phone):** the
  `@media (max-width:860px)` block hiding `.chat-side` sat BEFORE the base
  `.chat-side{display:flex}` rule — at equal specificity the later rule wins, so
  the sidebar stacked above the chat on mobile. Block moved after the base rules;
  compact mobile header added (chip hidden, name/meta truncate, ↻ restart icon in
  the header since the sidebar's "Start over" is hidden there).
- **Comfort-zoom viewport overflow (the real "timer not visible" cause):** the
  default text size A++ sets `zoom:1.12`, which multiplies `100dvh` at render —
  the page overflowed ~12% and the chat header (with the clock) scrolled off
  screen. Heights are now `calc(100dvh / var(--zoom, 1))`.
- **Composer below the fold once the thread grew:** `.chat-main`/`.chat-scroll`/
  `.chat-side` lacked `min-height:0`, letting grid/flex min-content expansion
  override the fixed chat height. Fixed.
- **Ask chips:** one swipeable row on phones (3 stacked chips ate half the
  screen); a similarity filter stops suggesting a question the participant
  already asked (≥3 shared content words); restart fab hidden in chat at all
  widths (it covered the chips row on mobile).
- **IOS/FSCS circles:** end anchors on one balanced line + centered legend (the
  84px `.sv-scale-end` cap had crushed them into one-word-per-line stacks).
  Wording untouched; monotonic overlap re-verified visually (smoke item v).
- **Free-continuation intro** in body type instead of monospace.
- `LLM_MAX_TOKENS` default raised to **16384** (user question 2: it is a safety
  ceiling, not a quality knob — but on reasoning models it also bounds hidden
  thinking, so generous headroom genuinely helps reply quality; the 90s timeout
  bounds runaway calls; visible length stays prompt-ruled).

## Iteration 2 — interaction quality

- **Slow-reply patience note:** gpt-5.1 reasoning can take 15-40s; bare typing
  dots that long read as a hang. After ~9s a quiet "Taking a moment — a
  thoughtful reply can take a little while." appears under the dots (calm
  wording only, §16). Shared by stage B, stage C, free continuation.
- **Auto-growing composers** (all three chats): the textarea grows with content
  (1 line → 200px) instead of scrolling inside a cramped single row.
- **Landing header button "Sign in" → "Begin"** (no account system exists; the
  label promised one).

## Iteration 3 — live verification + this record

- Deployed and verified live on Railway (markers in served assets + healthz).
- Word version of this record generated (`Change_Record_2026-06-11.docx`) and
  delivered in chat; both files live in the repo root.

## Answers to the user's two questions (2026-06-11)

1. **Railway `DATABASE_URL`:** the Postgres service's own URL is
   `postgresql://postgres:…@postgres.railway.internal:5432/railway`. On the APP
   service (not the Postgres service), set the variable `DATABASE_URL` to the
   reference `${{Postgres.DATABASE_URL}}` — Variables → New/edit Variable →
   "Add Reference" → pick Postgres → DATABASE_URL. Pasting the literal URL also
   works but breaks if the DB password is ever rotated; the reference never
   does. (If the Postgres service is named differently, use that name in the
   reference.) The DB password was pasted in chat — fine for now, rotate before
   fielding along with both tokens.
2. **Can the model token cap be unlimited?** There is no "unlimited" — providers
   cap output per call — but the new default (16384) is effectively unlimited
   for replies this size. Above ~the model's max it would be rejected. Bigger
   caps do NOT make replies better or longer by themselves (the cap only does
   anything when hit); on reasoning models they DO buy thinking headroom, which
   is why 16384 rather than 4096. Quality levers that actually matter here: the
   prompts (§8), temperature, and the model itself.

## 9. ⚠ ACTION REQUIRED in Railway (cannot be done from code)

1. Railway project → if there is **no Postgres service**, add one (New → Database →
   PostgreSQL).
2. Open the **app service → Variables** → set `DATABASE_URL` to a **reference**:
   `${{Postgres.DATABASE_URL}}` (replace the current localhost value).
3. Redeploy / wait for restart, then check
   `https://<domain>/healthz` → expect `"db": true`.
4. **Rotate `ADMIN_TOKEN` and `RESULTS_TOKEN`** — both were pasted in chat again and
   must be treated as compromised once fielding approaches.

---

## How to fold this batch into the spec documents (instructions for a future Claude)

The two living spec files are **Project Brief v4.1** and **Build Plan v5.1** (Kangzhi
has them; they are not in this repo). When updating them:

1. **Build Plan §12 (model config).** Add: "`LLM_MAX_TOKENS` (default 16384) is a
   safety ceiling on output tokens; reasoning models count hidden thinking tokens
   against it, so never set it near the intended reply length.
   `finish_reason=length` is logged as a warning."
2. **Build Plan §6 / Appendix C+D (Stage-C prompts).** Replace the PACING paragraph
   with the new hard rule (≤2 short paragraphs, ~60–110 words, one idea/scene, ≤1
   question, explicitly no exceptions for multi-part questions), note the rule now
   also opens both prompts as an overriding constraint, and document the per-call
   trailing system reminder (server-side, phase-c only, not in transcripts). All of
   it is intentionally **identical across main and baseline** so reply length is
   not a condition confound. Bump the prompt version tag.
3. **Build Plan §7 (Screen 7 — chat) and §11.4.** Document the "Ideas to ask" chips:
   fixed neutral 18-item pool, 3 shown after every reply, no repeats until the pool
   recycles, identical across conditions, free typing always available. Rationale:
   fatigue reduction + question breadth, without a model-generated (condition-
   dependent) nudge.
4. **Build Plan §7 (Screen 7) clock.** "The count-up clock is shown in the header AND
   the composer footer." (§16's no-countdown rule unchanged.)
5. **Build Plan §0 / §16a (comfort defaults).** Default reading font is now **Sans**
   (was Serif); comfort storage key is `thesis_comfort_v2`.
6. **Build Plan §7 Screen 8 / §3.9b (free continuation).** "The Stage-C transcript is
   re-displayed read-only above the divider; only new turns are logged to
   `freeContinuation`." (Analysis separation unchanged.)
7. **Build Plan §0 (persistent restart chrome).** "On desktop chat the restart
   control lives in the sidebar ('Start over', confirm-guarded); the floating fab
   appears on all other screens and on mobile chat."
8. **Build Plan §13b (ops/health).** `/healthz.db_detail` now includes `reason`
   (unwrapped) and `hint`; the admin dashboard shows a DB-down banner. Add the
   Railway runbook from §9 above.
9. **Brief (method section).** If the Brief quotes the Stage-C prompts or describes
   the chat UI, mirror items 2–4; the suggestion-chip pool should be listed verbatim
   in an appendix since it is participant-facing stimulus material.
10. **Build Plan §3 (chip pool spec).** Record the chip mechanics: fixed 18-item
   neutral pool, 3 shown after every reply, no repeats until the pool recycles, a
   similarity filter suppresses ideas overlapping questions the participant
   already asked (≥3 shared content words), single swipeable row on mobile.
11. **Build Plan §16 additions (batch 2).** (a) Typing indicator gains a calm
   patience note after ~9s ("Taking a moment — a thoughtful reply can take a
   little while."); (b) composers auto-grow 1 line → 200px; (c) landing header
   button reads "Begin" (was "Sign in"); (d) restart placement in chat: sidebar
   "Start over" (desktop) / header ↻ (mobile), floating fab on all other screens.
12. **Build Plan §16c (cross-browser × comfort).** Document the zoom interaction:
   comfort text sizes use CSS `zoom`, which multiplies viewport-unit lengths at
   render — all full-height layouts must use `calc(100dvh / var(--zoom, 1))`, and
   chat columns need `min-height: 0`. (This was the root cause of the hidden
   header/clock and of the composer dropping below the fold.)
13. **Build Plan §17 step 3b (smoke checklist).** Add: run `test/ui_walk.mjs`
   (Playwright, mocked backend, 34 screenshots desktop + mobile) and eyeball the
   output before fielding; items (v) and (vi) are covered by it.

Constraints honoured in this batch (do not regress): Andrea's reflective/direct
prompts untouched; condition routing / profile isolation untouched; instrument
wording and scales untouched; consent gating untouched; time policy (20/30 min, count-
up only) untouched; participant-flow copy kept neutral (no personalization promises).
