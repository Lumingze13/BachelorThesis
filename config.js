// Frontend runtime config.
// THESIS_API_BASE is the origin of the backend API (Railway). Leave empty for
// same-origin / local development; set to the Railway URL for the Vercel build.
window.THESIS_API_BASE = '';
